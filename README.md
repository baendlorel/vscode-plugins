# kt-packages

A pnpm-based monorepo for all packages in `packages/*`.

## Workspace

- Package manager: `pnpm`
- Workspace config: `pnpm-workspace.yaml`
- Package location: `packages/*`

## Common Commands

```bash
pnpm install
pnpm build
pnpm test
pnpm lint
```
